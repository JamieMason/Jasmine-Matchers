{
  "devDependencies": {
    "jasmine-expect": "*",
    "protractor": "5.4.2"
  },
  "private": true,
  "scripts": {
    "postinstall": "webdriver-manager update",
    "start": "webdriver-manager start",
    "test": "protractor conf.js"
  }
}
